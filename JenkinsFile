pipeline {
  agent rust-agent
  stages {
    stage('clone') {
      steps {
        sh 'git clone https://github.com/cardze/Rust-container.git
        }
}
    stage('test') {
      steps {
        sh 'python3 -m pytest --maxfail=1 --disable-warnings -q'
      }
    }
  }
}